
<td  id="snsMenu">
<div id="snsMenuContainer">
<div class="leftMenu">
    <%= render :partial => "sns/admin/profile_configs/editing_menu", :locals => {:current => @edit_current }%>
</div>

</div>
</td>

<td id="snsContent" colspan="2" class="configs">

<div class="bread_crumbs">

</div>
<div class="menu">
  <div class="leftMenu">
    <h2 class="mail_notice">メール通知設定</h2>
    <% if flash[:notice] %><div class="notice"><%= flash[:notice] %></div><% end %>
<table class="show">
  <tr>
    <th>友達申請があった</th>
    <td><%=Sns::Profile.notice_show(@item.proposal)%></td>
  </tr>
  <tr>
    <th>友達申請が承認された</th>
    <td><%=Sns::Profile.notice_show(@item.recognized)%></td>
  </tr>
  <tr>
    <th>プロジェクトに招待された</th>
    <td><%=Sns::Profile.notice_show(@item.invite)%></td>
  </tr>
  <tr>
    <th>プロジェクトへの招待を承認された</th>
    <td><%=Sns::Profile.notice_show(@item.invite_recog)%></td>
  </tr>
  <tr>
    <th>プロジェクトに更新があった</th>
    <td><%=Sns::Profile.notice_show(@item.project)%></td>
  </tr>
  <tr>
    <th>管理しているプロジェクトに退会申請があった</th>
    <td><%=Sns::Profile.notice_show(@item.leave)%></td>
  </tr>
  <tr>
    <th>プロジェクトへの退会申請が承認された</th>
    <td><%=Sns::Profile.notice_show(@item.leave_recog)%></td>
  </tr>
  <tr>
    <th>プロジェクトへの退会申請が却下された</th>
    <td><%=Sns::Profile.notice_show(@item.leave_refuse)%></td>
  </tr>
</table>
<%=link_to("編集する",edit_sns_notice_path(@item), :class=>"button")%>
  </div>
</div>
</td>