<td id="snsMenu">
<div id="snsMenuContainer">
<div class="profileContainer">
<%= render :partial => "sns/admin/base/project_menu"%>
</div>

</div>

</td>

<td id="snsContent" colspan="2" class="project">

<div class="bread_crumbs">

</div>

<div class="menu">
  <div class="leftMenu">
    <%= render :partial => "header" %>
    <%= render :partial => "sns/admin/files/show", :locals => {:is_project=>1} %>
  <div class="myFunction">
    <%if @item.is_editable? || @project.is_admin?%>
       <%=link_to "削除する",sns_project_file_path(@project_code,@item),:method=>:delete,
  :class => :delete, :title => "削除する", :confirm=>"このファイルを削除します。よろしいですか？" %>
     <%end%>
  </div>
  </div>
</td>