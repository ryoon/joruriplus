
<td id="snsMenu">
<%if @created_user.account == Core.profile.account%>
<div id="snsMenuContainer">
<div class="profileContainer">
  <%= render :partial => "sns/admin/base/profile_menu"%>
</div>
</div>
<%else%>
  <%= render :partial => "sns/admin/base/user_menu"%>
<%end%>
</td>

<td id="snsContent" colspan="2">

<div class="bread_crumbs">

</div>

<div class="menu">
  <div class="leftMenu">
    <%=link_to "ファイルを追加", new_sns_user_file_path(@created_user.account) if @u_role%>
<%if @items.blank?%>
表示するファイルが存在しません。
<%else%>
  <table style="width: 100%;" class="show">
  <%for item in @items%>
  <tr><td>
    <%= link_to("#{item.display_file_name}",sns_file_path(@created_user.account,item),:class => file_icon_class(item.file_name) ) %>
    <%= "（#{item.range_show}）"%>
   </td></tr>
  <%end%>
  </table>
<%= paginate @items %>
<%end%>
  </div>
</div>
</td>
