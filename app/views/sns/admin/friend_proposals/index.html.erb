
<td id="snsMenu">
<div id="snsMenuContainer">
<div class="profileContainer">
  <%= render :partial => "sns/admin/base/profile_menu"%>
</div>

</div>
</td>
<td id="snsContent" colspan="2" class="friendProposals">


<div class="bread_crumbs">

</div>

<div class="menu">
  <div class="leftMenu">
    <h2 class="friendProposals">申請情報</h2>
    <% if flash[:notice] %><div class="notice"><%= flash[:notice] %></div><% end %>
<%
s_ind_select = [["受信","to"],["送信","fr"]]
s_kind_select = [
    ["友達申請", "friend"],
    ["プロジェクト参加希望申請", "join"],
    ["プロジェクト招待","invite"],
    ["プロジェクト退会申請", "leave"],
    ["スケジュール参加依頼", "schedule"],
    ]
%>
<% form_tag('', :method => :get, :class => 'searchGgroup', :name=>'frmSearch') do %>
<%= select_tag('cl', options_for_select(s_ind_select, @current) , :onchange=>"submit(frmSearch.form)") %>
<%= select_tag('kind', options_for_select(s_kind_select, @kind) , :onchange=>"submit(frmSearch.form)") %>
<%end%>

<% if @current == "to" %>
  <%= render :partial => "received_index", :locals => {:current => @current }%>
<%elsif  @current == "fr"%>
  <%= render :partial => "send_index", :locals => {:current => @current }%>
<%end%>
  </div>
</div>
</td>